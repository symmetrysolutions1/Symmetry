# Deployment Artifacts

This directory stores deployment plans and deployment artifacts for Symmetry.

Recommended subdirectories:

- `plans/<chain-key>/`
- `records/<chain-key>/`

Each deployment should generate:

- one pre-deployment plan
- one post-deployment artifact
- one ownership handoff confirmation note
